<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage"
             Title="ホーム">

    <ScrollView>
        <VerticalStackLayout
            Padding="24,32"
            Spacing="16">
            <Button
                Text="オーダーする"
                Clicked="OnOrderClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="TODOリストを開く"
                Clicked="OnTodoClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="習慣記録"
                Clicked="OnHabitRecordClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="レシート印刷"
                Clicked="OnReceiptPrintClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="カスタマーディスプレイ（DM-D30）"
                Clicked="OnCustomerDisplayClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
